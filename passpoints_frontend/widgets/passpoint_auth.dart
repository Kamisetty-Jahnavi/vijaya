import 'package:flutter/material.dart';

class PassPointAuth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PassPoints Authentication'),
      ),
    );
  }
}
